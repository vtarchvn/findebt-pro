# VietQR

URL dùng `img.vietqr.io`, bank code, account number, account name, amount = outstanding và nội dung `FIN {Ma_DT} {Ma_CT}`. Nếu outstanding bằng 0 hoặc không phải khoản phải thu, server trả `null`. Không lưu token ngân hàng.
